#include <iostream>
#include <vector>

using namespace std;

int checkWin();

int main() {
  const bool XT = true,
    OT = false;
  const char N = ' ',
    X = 'X',
    O = 'O';
  bool turn = XT;
  vector<vector<char>> board{{N, N, N},
                             {N, N, N},
                             {N, N, N}};
  


  return 0;
}

int checkWin(vector<vector<char>> board) {
  
}
